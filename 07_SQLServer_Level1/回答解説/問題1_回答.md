# 練習問題1 回答・解説


<div class="bug-summary">
<div class="bug-card high">
<div class="bug-no">Bug #1</div>
<div class="bug-title">OrdersにFOREIGN KEY制約がない</div>
<div class="bug-severity">重要度: 高</div>
</div>
<div class="bug-card high">
<div class="bug-no">Bug #2</div>
<div class="bug-title">SubtotalがINT型（DECIMAL必須）</div>
<div class="bug-severity">重要度: 高</div>
</div>
<div class="bug-card high">
<div class="bug-no">Bug #3</div>
<div class="bug-title">StockQuantityにCHECK制約なし</div>
<div class="bug-severity">重要度: 高</div>
</div>
</div>

## バグ一覧（3件）

---

### バグ1: OrdersテーブルのCustomerIDに外部キー制約がない

**該当仕様**: BR-005 外部キー制約の必須設定
> **CustomerID、ProductID、OrderIDの参照関係には、必ず外部キー制約（FOREIGN KEY）を設定すること。**

また、テーブル設計「3.3 Orders」:
> CustomerID: **FOREIGN KEY → Customers(CustomerID)**

**該当コード**: `CreateTables.sql` Ordersテーブル定義

```sql
-- 現在の実装（バグ）
CREATE TABLE Orders (
    OrderID      INT            PRIMARY KEY IDENTITY(1,1),
    CustomerID   INT            NOT NULL,
    OrderDate    DATE           NOT NULL DEFAULT GETDATE(),
    TotalAmount  DECIMAL(12,2)  NOT NULL DEFAULT 0,
    Status       NVARCHAR(20)   NOT NULL DEFAULT N'未処理'
    -- ← CustomerIDへの外部キー制約がない
);
```

**修正コード**:
```sql
CREATE TABLE Orders (
    OrderID      INT            PRIMARY KEY IDENTITY(1,1),
    CustomerID   INT            NOT NULL,
    OrderDate    DATE           NOT NULL DEFAULT GETDATE(),
    TotalAmount  DECIMAL(12,2)  NOT NULL DEFAULT 0,
    Status       NVARCHAR(20)   NOT NULL DEFAULT N'未処理',
    CONSTRAINT FK_Orders_Customers
        FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID)
);
```


<div class="concept-box">
<div class="concept-title">テーブル間の参照関係</div>
<svg viewBox="0 0 600 200" style="max-width:600px;margin:0 auto;">
<rect x="20" y="30" width="150" height="80" rx="8" fill="#e8f0fe" stroke="#005BAB" stroke-width="2"/>
<text x="95" y="55" text-anchor="middle" font-weight="bold" fill="#005BAB" font-size="14">Customers</text>
<text x="95" y="80" text-anchor="middle" fill="#333" font-size="12">CustomerID (PK)</text>
<text x="95" y="97" text-anchor="middle" fill="#666" font-size="11">Name, Phone...</text>
<rect x="225" y="30" width="150" height="80" rx="8" fill="#fff5f5" stroke="#dc3545" stroke-width="2" stroke-dasharray="6,3"/>
<text x="300" y="55" text-anchor="middle" font-weight="bold" fill="#dc3545" font-size="14">Orders</text>
<text x="300" y="80" text-anchor="middle" fill="#333" font-size="12">CustomerID</text>
<text x="300" y="97" text-anchor="middle" fill="#dc3545" font-size="11">FK制約なし [Bug#1]</text>
<rect x="430" y="30" width="150" height="80" rx="8" fill="#e8f0fe" stroke="#005BAB" stroke-width="2"/>
<text x="505" y="55" text-anchor="middle" font-weight="bold" fill="#005BAB" font-size="14">OrderDetails</text>
<text x="505" y="80" text-anchor="middle" fill="#333" font-size="12">OrderID (FK)</text>
<text x="505" y="97" text-anchor="middle" fill="#666" font-size="11">ProductID (FK)</text>
<line x1="170" y1="70" x2="225" y2="70" stroke="#dc3545" stroke-width="2" stroke-dasharray="6,3"/>
<text x="197" y="63" text-anchor="middle" fill="#dc3545" font-size="18">?</text>
<line x1="375" y1="70" x2="430" y2="70" stroke="#005BAB" stroke-width="2"/>
<text x="402" y="63" text-anchor="middle" fill="#005BAB" font-size="10">FK</text>
<text x="300" y="145" text-anchor="middle" fill="#dc3545" font-size="12" font-weight="bold">存在しないCustomerIDの注文が登録可能</text>
</svg>
</div>

**レビュー指摘例**:
```
【指摘箇所】CreateTables.sql: OrdersテーブルのCREATE TABLE定義
【分類】仕様不一致
【重要度】高
【内容】設計仕様書 BR-005 および テーブル設計3.3 では、
        Orders.CustomerID に Customers(CustomerID) への外部キー制約を
        設定すると定義されているが、実装では外部キー制約が定義されていない。
        存在しないCustomerIDの受注レコードが登録可能な状態であり、
        データの整合性が保証されない。
【修正案】OrdersテーブルにCONSTRAINT FK_Orders_Customers
        FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID)
        を追加してください。
```

---

### バグ2: OrderDetailsのSubtotalカラムがINT型

**該当仕様**: テーブル設計「3.4 OrderDetails」
> Subtotal: **DECIMAL(12,2)** NOT NULL  小計 = Quantity x UnitPrice

また、非機能要件:
> **金額系カラムはすべてDECIMAL型を使用すること（INT不可）**

**該当コード**: `CreateTables.sql` OrderDetailsテーブル定義

```sql
-- 現在の実装（バグ）
CREATE TABLE OrderDetails (
    DetailID    INT          PRIMARY KEY IDENTITY(1,1),
    OrderID     INT          NOT NULL,
    ProductID   INT          NOT NULL,
    Quantity    INT          NOT NULL CHECK (Quantity > 0),
    UnitPrice   DECIMAL(10,2) NOT NULL,
    Subtotal    INT          NOT NULL,   -- ← INT型になっている
    ...
);
```

**修正コード**:
```sql
    Subtotal    DECIMAL(12,2) NOT NULL,
```


<div class="comparison">
<div class="ng">
<div class="label">[NG] Subtotal INT</div>
<code>2500.50 x 3 = 7501.50
→ INT型で小数切捨て → 7501
→ 0.50円の誤差が蓄積</code>
</div>
<div class="ok">
<div class="label">[OK] Subtotal DECIMAL(12,2)</div>
<code>2500.50 x 3 = 7501.50
→ DECIMAL型で正確に保存
→ 金額計算に誤差なし</code>
</div>
</div>

**レビュー指摘例**:
```
【指摘箇所】CreateTables.sql: OrderDetailsテーブル Subtotalカラム
【分類】仕様不一致
【重要度】高
【内容】設計仕様書 テーブル設計3.4 では Subtotal のデータ型は
        DECIMAL(12,2) と定義されているが、実装では INT 型になっている。
        Subtotal = Quantity x UnitPrice であり、UnitPriceがDECIMAL(10,2)のため
        小数を含む結果が発生する（例: 2500.50 x 3 = 7501.50）。
        INT型では小数部分が切り捨てられ、金額の不整合が発生する。
        また、非機能要件にて「金額系カラムはすべてDECIMAL型」と明記されている。
【修正案】SubtotalのデータをINTからDECIMAL(12,2)に変更してください。
```

---

### バグ3: Products.StockQuantityにCHECK制約がない

**該当仕様**: テーブル設計「3.2 Products」
> StockQuantity: INT NOT NULL, **CHECK (StockQuantity >= 0)** 在庫数（0以上）

また、BR-003:
> **在庫数が注文数量を下回る場合はエラーとする**

**該当コード**: `CreateTables.sql` Productsテーブル定義

```sql
-- 現在の実装（バグ）
CREATE TABLE Products (
    ProductID      INT            PRIMARY KEY IDENTITY(1,1),
    ProductName    NVARCHAR(200)  NOT NULL,
    Category       NVARCHAR(50)   NOT NULL,
    UnitPrice      DECIMAL(10,2)  NOT NULL,
    StockQuantity  INT            NOT NULL
    -- ← CHECK制約がない
);
```

**修正コード**:
```sql
CREATE TABLE Products (
    ProductID      INT            PRIMARY KEY IDENTITY(1,1),
    ProductName    NVARCHAR(200)  NOT NULL,
    Category       NVARCHAR(50)   NOT NULL,
    UnitPrice      DECIMAL(10,2)  NOT NULL,
    StockQuantity  INT            NOT NULL CHECK (StockQuantity >= 0)
);
```

**レビュー指摘例**:
```
【指摘箇所】CreateTables.sql: ProductsテーブルStockQuantityカラム
【分類】仕様不一致
【重要度】高
【内容】設計仕様書 テーブル設計3.2 では StockQuantity に
        CHECK (StockQuantity >= 0) 制約を設定すると定義されているが、
        実装ではCHECK制約が定義されていない。
        マイナスの在庫数がデータベースレベルで入力可能な状態であり、
        BR-003（在庫チェック）の前提が崩れる。
        アプリケーション側のバグやSQL直接実行でマイナス在庫になるリスクがある。
【修正案】StockQuantityカラムに CHECK (StockQuantity >= 0) 制約を追加してください。
```

---

## バグの見つけ方のコツ

1. **仕様書のテーブル定義の「制約」列を1つずつ照合する**
   - PRIMARY KEY、FOREIGN KEY、NOT NULL、CHECK、DEFAULTを漏れなく確認
   - 今回のバグ1とバグ3はこの照合で発見できる

2. **データ型を仕様書と突き合わせる**
   - 特に金額系カラムのDECIMAL指定は厳密に確認
   - INT/DECIMAL/NVARCHARなどの型の違いは見落としやすい
   - 今回のバグ2はこの照合で発見できる

3. **外部キー制約の有無は特に重要**
   - 外部キー制約がないと、存在しないIDの参照が可能になる
   - テーブル間の参照整合性はデータベースの根幹

4. **CHECK制約は「最後の砦」**
   - アプリケーション側でバリデーションしていても、DB直接操作で不正値が入る可能性がある
   - CHECK制約でデータベースレベルでの防御を行うことが重要

## 次の問題

この問題が解けたら `問題2` に進んでください。
次の問題ではSQLクエリのレビューに挑戦します。
